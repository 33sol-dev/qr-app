<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= product %> Details</title>
  <style>
    body { font-family: sans-serif; max-width: 360px; margin: auto; padding: 1rem; }
    label { display:block; margin-top:1rem; }
    input { width:100%; padding:.5rem; font-size:1rem; }
    button { margin-top:1.5rem; padding:.75rem; width:100%; font-size:1rem; }
  </style>
</head>
<body>
  <h1><%= product %> (Batch <%= batch %>)</h1>
  <form method="POST" action="/<%= slug %>">
    <label>Name <input name="name" required></label>
    <label>Phone <input name="phone" required pattern="[0-9]{10}"></label>
    <label>Pincode <input name="pincode" required pattern="[0-9]{6}"></label>
    <button type="submit">Continue</button>
  </form>
  
</body>
</html>
